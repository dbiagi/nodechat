

function Chat(e) {
    
    var __constructor = function (e) {
        if (!(e instanceof Element)) {
            throw 'The e parameter needs to be a DOM Element.'
        } else if (e.tagName !== 'DIV') {
            throw 'The element needs to be a DIV'
        }
    }(e)
    
    
}